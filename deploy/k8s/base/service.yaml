apiVersion: v1
kind: Service
metadata:
  name: aegis-rpc
  namespace: aegis-rpc
  labels:
    app.kubernetes.io/name: aegis-rpc
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: aegis-rpc
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
